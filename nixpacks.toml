[phases.setup]
nixPkgs = ['nodejs_18']

[phases.install]
cmds = ['npm install', 'cd frontend && npm install']

[phases.build]
cmds = ['cd frontend && npm run build']

[start]
cmd = 'node server-railway.js'
