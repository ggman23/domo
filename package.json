{
  "name": "domo-app",
  "version": "1.0.0",
  "description": "Application de domotique pour appareils Smart Life / Tuya",
  "type": "module",
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "tunnel": "node scripts/check-tunnel.js || bash scripts/tunnel.sh",
    "tunnel:windows": "scripts\\tunnel.bat",
    "start": "node server-railway.js",
    "build": "cd frontend && npm install && npm run build",
    "railway:build": "npm install && cd frontend && npm install && npm run build",
    "vercel-build": "cd frontend && npm install && npm run build"
  },
  "keywords": ["domotique", "smart-home", "tuya", "smart-life"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "axios": "^1.6.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
